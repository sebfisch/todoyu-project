<?xml version="1.0" encoding="UTF-8"?>
<form>
	<hiddenFields>
		<field name="id" />
	</hiddenFields>

	<fieldsets>
		<fieldset name="main">
			<legend>Main userrole Fields</legend>
			<elements>

				<field type="textinput" name="title">
					<label>core.title</label>
					<required />
				</field>

				<field type="textinput" name="rolekey">
					<label>project.userrole.attr.rolekey</label>
					<required />
				</field>

			</elements>
		</fieldset>

	</fieldsets>
</form>