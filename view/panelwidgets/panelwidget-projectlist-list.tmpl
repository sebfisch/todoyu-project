<ul id="panelwidget-{$id}-list">
	{foreach $projects project}
		<li id="panelwidget-{$id}-project-{$project.id}" class="bcStatus{$project.status}">
			<a href="javascript:void(0)" title="ID: {$project.id}">{$project.company} - {$project.title}</a>
		</li>
	{else}
		{Label panelwidget-projectlist.list.empty}
	{/foreach}
</ul>