<div class="left description">{cleanHtml $description}</div>
<div class="right properties">
	<dl>
	{foreach $data info}
		<dt class="{$info.className}">{Label $info.label true}</dt>
		<dd {if $info.id}id="{$info.id}" {/if}class="{$info.className}">{$info.value}&#8203;</dd>
	{/foreach}
	</dl>
</div>